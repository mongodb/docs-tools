version:
  published:
    - '1.0'
  active:
    - '1.0'
  stable: '1.0'
  upcoming: null
git:
  branches:
    manual: 'master'
    published:
      - 'master'
      - 'v1.0'
      # the branches/published list **must** be ordered from most to
      # least recent release.
...